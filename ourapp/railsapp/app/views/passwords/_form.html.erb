<%= form_with(model: password) do |form| %>
  <% if password.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(password.errors.count, "error") %> prohibited this password from being saved:</h2>

      <ul>
        <% password.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :test_user_id, style: "display: block" %>
    <%= form.text_field :test_user_id %>
  </div>

  <div>
    <%= form.label :hashed_password, style: "display: block" %>
    <%= form.text_field :hashed_password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
